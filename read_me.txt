1. install node: https://nodejs.org/en/

optional:
	ubuntu:
		"apt install nodejs"
		"wget -qO- https://raw.githubusercontent.com/nvm-sh/nvm/v0.37.2/install.sh | bash" // nodejs verion manager, to change between arbitrary versions of nodejs
		"apt install npm" // node package manager to install modules/code others wrote